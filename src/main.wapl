use "./std/allstd.wapl"

fn __main_argc_argv(i32 argc,ptr:ptr:char argv):i32{

    #=(v,VecT_rangei64(0,11,1),VecT);
    #=(it,VecT_iter(v),Iterator);
    loopif:(is_some(iter_peek(&_(it)))){
        #=(i,*_(unwrap(iter_next(&_(it))),i64),i64);
        iter_next(&_(it));
        printf("%lld",i);
    }
    VecT_free(&_(v));
    #=(n,_,i32);
    scanf("%d",&_(n));

    printf("Hello, World!%d\n",n);
    return 0s;
}