use "./std/allstd.wapl"

declare printf(ptr:char,...):i32;

fn main():i32{
    clear_cli_screen();
    #=(readed,read_text_file(input_str()),String);

    printf("%s\n",.(readed,data));
    
    String_free(&_(readed));

    return 0s;
}