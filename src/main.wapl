use "./std/allstd.wapl"

fn voi():void{
    if(true){
        println("voi");
        return VOID();
    }
    println("d");
}

fn main():i32{
    #=(ans,0,i64);
    #=(n,_,i64);
    scanf("%lld",&_(n));
    //#=(a,input_i64(n),*:i64);
    voi();

    println(format("%lld",ans));
    //free(pmove(a));
    return 0s;
}