use "./std/allstd.wapl"

fn main():i32{
    #=(ans,0,i64);
    #=(a,String_from(input_str()),String);
    #=(clone,String_clone(a),String);

    String_free(&_(a));
    
    println(format("%s",.(clone,data)));

    String_free(&_(clone));

    return 0s;
}